apiVersion: v1
kind: Service
metadata:
  name: rdicidr-service
  namespace: production
spec:
  type: ClusterIP
  selector:
    app: rdicidr
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 80
